import { User } from './User'
import { UserToken } from './UserToken'
import { UserData } from './UserData'
import { Session } from './Session'

const entities = [User, UserToken, UserData, Session]

export {
    entities,
    User,
    UserToken,
    UserData,
    Session
}
